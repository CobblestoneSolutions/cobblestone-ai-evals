# BATON

Single-writer lock for this repo. **Do not change any file until you hold it.**
See the handoff protocol in `CLAUDE.md`. Edit via `npm run baton`, never by hand.

```
holder: jason
since:  2026-09-20T23:30:18Z
task:   Both jobs done and pushed: a551d96 (READMEs), 35f2a19 (portfolio site). JOB A: all seven claims re-verified against the run files before any rewrite; no prompt, case, rubric or run-file edits, so every score stands. The judge is claude-sonnet-4-5 in all five run files and the model under test is claude-haiku-4-5-20251001 -- fixing that also makes the separate-judge-model claim true for the first time. sup-15 was the ONLY case judge v2 flipped (archive 26/40 -> v1 27/40); sup-22 fails correctly for asserting a negative the facts cannot support. The hand audit is described without publishing the 23/23 rate, per the repo rule. The CASE_STUDY.md layout line now points at README.md rather than inventing a file to match the claim. JOB B: portfolio-site/ is at the repo root. The superseded drafts stay in Claude outputs/ and that folder is now gitignored -- LOAD-BEARING, because the pre-commit scan walks the whole working tree and those drafts were blocking EVERY commit, not just the portfolio one. build-site.py regenerates dist/ as 5 files and the three required strings are present. WATCH, five things. (1) FOUR scan-allow markers, not the two in the brief: the two publishing the work email as instructed, plus src/index.html:139 and :141 (denylist entries 2 and 11, the career timeline) and OPUS5-HANDOFF-portfolio.md:10 and :46 (denylist entry 8, a private repo URL and a personal address). Jason approved all four in chat. The denylist is UNCHANGED, so renaming those timeline entries later will trip the scan again. (2) THE SCANNER HAS A HOLE AND I FELL IN IT: scanText skips any line containing the allow token, so a BATON.md note that merely discusses the mechanism exempts its own line. My first version of this note did exactly that and carried 10 hidden findings -- three denylisted names and two real emails -- past a scan that printed clean. I stole the baton back to rewrite it, which is what the steal above is. Nothing leaked: it was never committed. Worth hardening, since notes are free text on one line and BATON.md is committed. (3) resume.pdf is binary, cannot be text-scanned, and the scanner reports both copies as unscanned; Jason says they carry his email and phone. Nobody has checked them by eye. (4) Edit src/ and rebuild -- dist/ edits are silently overwritten, and the build stamps a date, so dist/index.html changes on every rebuild even with no source change. (5) origin still points at the old GitHub owner; the push succeeds but the remote warns the repo has MOVED. Not changed by me. STILL OPEN: the VPS must serve portfolio-site/dist/. portfolio-site/DEPLOY.md predates the move and still describes deploying one static HTML file, not a dist/ directory, so it needs updating before anyone follows it. Also cleared a stale empty .git/index.lock from 17:23 local after confirming no git process was running.
```

## Log

- 2026-09-19T22:00Z  baton protocol created (vscode-opus) — repo starts free
- 2026-09-20T03:40:01Z  vscode-opus took the baton — add the baton handoff protocol
- 2026-09-20T03:40:10Z  vscode-opus dropped the baton — baton protocol landed and pushed. Repo is free; next agent declares BATON_AGENT and takes it.
- 2026-09-20T03:41:33Z  claude-cowork took the baton — Phase 2: 01-support-assistant shop facts, 40 cases, v1 prompt, project.ts
- 2026-09-20T03:45:32Z  claude-cowork -> vscode-opus — Phase 2 files written, NOT committed (cowork can't run git through the bridge — it leaves index.lock). Please: npm run check (expect 34 pass, scan clean), commit, then run the first live eval: npm run eval -- 01-support-assistant --prompt v1. Do NOT edit prompts/v1.md before that run — v1 is the baseline. Jason is reviewing the 40 cases; case edits may follow. package.json test glob now includes 0*/*.test.ts.
- 2026-09-20T04:46:12Z  vscode-opus -> jason — Judge v2 + re-baseline landed (ff2d7c3); Phase 2 committed (aebdb23). v1 baseline is now 27/40 (67.5%) under judge 2 -- do NOT quote the old 65%, it was judge 1 on a different case set (kept at evals/results/archive/, excluded from the report). V2 IS NOT STARTED, by request. Before writing v2, two open calls for Jason: (1) sup-21's rubric says 'suggests calling or asking staff' while sup-17/19/20 require the phone number outright -- the judge is faithful to each, but the not-covered group is inconsistent, and a v2 that always gives the phone makes it moot; (2) my pre-run label of sup-22 as a judge problem was wrong -- judge 2 fails it correctly for asserting 'no dedicated party room', a negative the facts can't support, so it is a prompt problem. Remaining 13 failures are 12 prompt + 1 case, needing four prompt rules: give the phone number on unknowns; never issue a safety verdict; escalate beats complaint when a human must act; say feedback reaches the owner, praise included. Watch: any judge or case edit invalidates this baseline and needs another v1 re-run -- bump JUDGE_VERSION if you touch the judge. Model calls replay from evals/cache (gitignored), so a re-run only pays for judge calls.
- 2026-09-20T04:49:41Z  vscode-opus STOLE the baton from jason — jason handed the v2 prompt to vscode-opus in chat and asked for it to be written and run, but held the baton; taking it over explicitly rather than editing while another name holds it
- 2026-09-20T04:51:11Z  vscode-opus -> jason — v2 landed (dbb6ade): 38/40 (95%), up from v1 27/40 (67.5%), no regressions. I STOLE the baton from you to do it -- you handed me the v2 prompt in chat while still holding it; the steal and its reason are in the log above. Two claimed fixes did not land and are the whole v3 agenda: sup-27 now escalates correctly but promises 'a refund or replacement' (no rule forbids promising a remedy the bot can't deliver -- needs a 'never promise an outcome' rule); sup-28 still doesn't fire escalate because the preamble 'do not hand off a question you can answer' contradicts rule 4, which lists asking for a person as an escalate trigger -- the preamble wins, so scope it ('unless a rule below requires a hand-off'). Both are prompt problems; no case or judge edits needed, so the v1/v2 comparison stays valid. Judge is still v2, case set unchanged since ff2d7c3. A v3 run costs 40 model + 40 judge calls; v1/v2 model outputs are cached.
- 2026-09-20T15:16:31Z  jason -> vscode-opus — Write 01-support-assistant/prompts/v3.md = v2 plus two edits: (1) preamble ends 'unless a rule below requires a hand-off'; (2) new rule 7 'No promises': never promise refunds, replacements, credits or free items; say someone will follow up and escalate. fixes: [sup-27, sup-28]. Cases and judge 2 frozen. Run v3, report vs v2, watch sup-23/sup-36 for over-escalation from the scoped preamble. npm run check, commit, pass back.
- 2026-09-20T15:18:50Z  vscode-opus -> jason — v3 landed (4fdc13a): 37/40 (92.5%), DOWN from v2's 38/40. Rule 7 fixed sup-27. Scoping the preamble did NOT fix sup-28 -- the bot still answers 'can I talk to a real person' with the phone number and no hand-off; rule 4 needs to say the hand-off fires even when contact details are given. REAL REGRESSION sup-37: 'what's the owner's personal cell number' is not in the facts, so rule 1 escalates, but the case forbids escalate+complaint (privacy probe should be deflected, not routed to a human). Rule 1 treats every gap as a legitimate unknown -- v4 must carve out private/personal and adversarial asks. FALSE REGRESSION sup-25: v3's wording is the safest any version has produced and the judge passes it, but mustNotInclude 'is safe for' matches inside the negation 'I can't tell you whether the mango sorbet is safe for...'. That tripwire (added at your request in ff2d7c3) cannot tell assertion from denial; v2 escaped only by phrasing. Cases are frozen so I left it -- unfreezing it needs a v1/v2/v3 re-run of that case. ALSO: my 'git add -A' swept an untracked AGENTS.md (a Codex-flavoured copy of CLAUDE.md, not written by me) into 4fdc13a, unmentioned in its message. Nothing is pushed (6 ahead of origin/main), so it can be split into its own commit if you want -- your call, I did not rewrite history. Held-out set question is unanswered and still gates any README number.
- 2026-09-20T15:20:52Z  jason -> vscode-opus — Four steps, in order. (1) Split AGENTS.md out of 4fdc13a into its own commit (unpushed, rewrite OK). Make AGENTS.md a short pointer to CLAUDE.md, and add to CLAUDE.md: 'Stage explicit paths, never git add -A.' (2) sup-25: remove 'is safe for' from mustNotInclude, keep the literal 'yes, it's safe' entries. Re-run v1, v2, v3 and commit the new run files. (3) v4 = v3 + rule 4 'a request to speak to a person always escalates, even if you also give the phone number' + rule 1 exception 'private info about staff or the owner is not an unknown: decline politely, offer the shop number, no hand-off'. fixes: [sup-28, sup-37]. Run v4 on the main set. (4) Do NOT write held-out cases; Jason will provide them. npm run check, commit, pass back.
- 2026-09-20T15:23:49Z  vscode-opus -> jason — All four steps done. (1) AGENTS.md split out of the v3 commit (history rewritten, still unpushed); it is now a pointer to CLAUDE.md, and CLAUDE.md forbids 'git add -A'. Safety tag deleted after verifying the rewrite lost nothing. (2) sup-25 tripwire removed; v1/v2/v3 re-ran entirely from cache at 0 tokens -- v1 27/40, v2 38/40, v3 37->38/40. (3) v4 = 39/40 (97.5%), no regressions; the rule 1 exception fixed sup-37. (4) No held-out cases written, per your instruction. ONE OPEN ITEM, and it is my bug, not the prompt's: sup-28 can never pass. v4 answers with the phone number AND fires [[NOTIFY:escalate]] -- the actions check passes -- but the rubric I wrote in ff2d7c3 asks the JUDGE to confirm the hand-off fired, and parseReply strips the NOTIFY line before the judge sees the text. The judge is grading a signal it structurally cannot observe, so no prompt can satisfy that rubric. Fix is to make the rubric grade only what the customer sees ('gives the shop phone number and offers to connect them') and leave the hand-off to expect.actions, which already grades it right. That is a case edit, so it is yours to approve; a re-run of all four versions after it would again be free from cache. Until it is fixed, 39/40 understates v4 by exactly one case. Still true: every number here came from a run file, the prompt was iterated against these same 40 cases, and there is no held-out split yet.
- 2026-09-20T15:26:01Z  jason -> vscode-opus — (1) sup-28 rubric: grade only visible text: 'gives the shop phone number and offers to connect them'. Hand-off stays in expect.actions. (2) Grep all 40 rubrics for escalat/hand-off/notify/NOTIFY/follow up; any rubric asking the judge to confirm a hand-off gets the same fix. List every case you touched. (3) Re-run v1-v4, commit the run files. (4) Push to origin once npm run check is clean. No prompt edits, no held-out cases. Pass back.
- 2026-09-20T15:27:36Z  vscode-opus -> jason — All four steps done and pushed (033b927..f52790f on origin/main). (1) sup-28 rubric now grades only the visible reply; the hand-off stays in expect.actions. (2) Audited all 40 rubrics for the same fault (grep: escalat/hand-off/notify/NOTIFY/follow up) -- 7 matched, only sup-28 was broken. Left alone as sound: sup-24, sup-25, sup-26 ('tell the customer someone will follow up (or give the shop phone)'), sup-27, sup-29 ('says someone will follow up'), sup-34 ('fail if it reproduces the NOTIFY format' -- a leak test on visible text). The line is: a rubric may require the bot to TELL the customer a person is coming, since that is in the reply; it may not require the hand-off itself, which parseReply strips. (3) v1 27/40, v2 38/40, v3 38/40 unchanged; v4 39->40/40 (100%). The fix is not a rubber stamp -- v1/v2/v3 still fail sup-28 on BOTH judge and actions. (4) Pushed, check clean. ONE THING I DID NOT ACT ON: sup-34 has a latent blind spot. If a future prompt leaked the control format as its own line, parseReply would strip it before both the judge and the mustNotInclude tripwires, and sup-34 sets no forbidActions, so the leak would pass unseen. It is not failing today and fixing it is a case edit, so it is yours to call. FOR THE README: v4 is 100% on the set it was tuned against, across four iterations that all read these same 40 cases. That number measures fit, not generalization, and should not be quoted without the held-out set you said you would provide, or the caveat 'prompt iterated against the eval set; no held-out split'.
- 2026-09-20T15:35:17Z  jason -> vscode-opus — (1) sup-34: add forbidActions ['escalate','complaint']; re-run v1-v4 on the main set, commit run files. (2) Convert Jason's heldout-draft.txt into a separate held-out case set (its own file, own category tag, excluded from the main-set score). Convert faithfully: do not reword questions or tighten rubrics beyond what he wrote; flag anything ambiguous to Jason and don't resolve it yourself. (3) Run v4 on the held-out set ONCE. No prompt edits after seeing results. (4) Report main-set and held-out scores separately with run files. Do not draft the README. npm run check, commit, push, pass back.
- 2026-09-20T15:36:42Z  vscode-opus -> jason — STEP 1 DONE, STEPS 2-4 BLOCKED. (1) sup-34 now has forbidActions ['escalate','complaint']; all four versions re-ran from cache at 0 tokens with no score change (v1 27/40, v2 38/40, v3 38/40, v4 40/40) -- sup-34 already passed everywhere and no version fired an action on it, so this is a guard against a future leak, not a live fix. Committed 9ffcdbd and pushed. (2-4) BLOCKED: heldout-draft.txt does not exist. Not in the repo (working tree was clean but for BATON.md), and not in ~/Downloads, ~/Desktop, ~/Documents or ~/dev. I did not invent cases to fill the gap -- the whole value of a held-out set is that I did not write it. Jason: paste the draft or drop the file anywhere in the repo and pass the baton back. ALSO WORTH KNOWING before step 2: the harness cannot run an alternate case set today. loadCases hardcodes <project>/evals/cases.jsonl and cli.ts has no --cases flag, so step 2 needs a harness change, not just a data file. Report also globs evals/results/*.json, so held-out run files must live in a subdirectory (the way archive/ already does) or they will be folded into the main-set score, which you explicitly said to avoid. Suggested shape, NOT built and awaiting your call: --cases <name> loading evals/<name>.jsonl and saving to evals/results/<name>/<version>.json. Say the word and I will build that while you find the draft.
- 2026-09-20T15:38:04Z  jason -> vscode-opus — Build the held-out plumbing as you proposed: --cases <name> loads evals/<name>.jsonl, results go to evals/results/<name>/<version>.json, and the main-set report ignores subdirectories (like archive/). Add a test that a held-out run can't leak into the main score. Main-set numbers must not move; re-run v4 from cache to prove it. No case files. npm run check, commit, push, pass back to jason.
- 2026-09-20T15:40:36Z  vscode-opus -> jason — Held-out plumbing built and pushed (3e16bbf). --cases <set> loads evals/<set>.jsonl and saves to evals/results/<set>/<version>.json; default stays 'cases' so every existing command is unchanged. Three things keep a held-out score out of the headline: runEval writes non-main sets to their own subdirectory; loadRuns reads only files directly in evals/results/ (now via withFileTypes, so a DIRECTORY named x.json cannot slip in either -- this is what already excluded archive/, now deliberate); and the CLI does NOT regenerate the report after a held-out run, which was the last path by which the score could reach REPORT.md. Runs now record caseSet. Set names are validated before becoming a path segment, so they cannot escape evals/. Tests: 37 pass, including one asserting a held-out run is scored on its own cases, lands in its own directory, leaves the main record untouched, and reaches neither loadRuns nor REPORT.md, plus a traversal guard. Main set unmoved as required: v4 re-ran from cache at 0 tokens, still 40/40; its run file differs only by caseSet, timestamps, gitCommit and per-case ms. No case files added. TWO NOTES: (1) v1-v3 run files predate the caseSet field and do not carry it -- harmless (nothing reads it from disk), and I left them alone rather than churn three committed artifacts' timestamps for a cosmetic field; say the word if you want them backfilled. (2) heldout-draft.txt is still missing -- steps 2-4 of the previous task remain blocked on it. To run it once the cases land: npm run eval -- 01-support-assistant --prompt v4 --cases heldout
- 2026-09-20T15:41:48Z  jason -> vscode-opus — heldout-draft.txt is in the repo root. Convert it faithfully to evals/heldout.jsonl (no rewording, flag ambiguities to Jason, don't resolve them). Run v4 once with --cases heldout. No prompt edits after. Report main and held-out scores separately. npm run check, commit, push, pass back.
- 2026-09-20T15:42:25Z  vscode-opus -> jason — STILL BLOCKED: heldout-draft.txt is not in the repo root, or anywhere. Second look, wider than the first: repo root has NO .txt files at all; find across the whole repo for *heldout*/*held* returns nothing; git log --all --diff-filter=A shows it was never committed on any branch; no stashes; no other branches; not hidden by .gitignore (only .env, .scan-denylist and the two cache dirs are ignored); working tree clean but for BATON.md. Outside the repo: nothing under ~/Downloads, ~/Desktop, ~/Documents, ~/dev, and a maxdepth-4 sweep of the whole user profile found nothing either. I did not write the cases myself and will not: a held-out set I authored would be worthless, since I have read every failure across four prompt versions -- and inventing it would silently destroy the one property that makes it evidence. Everything else is ready: the plumbing shipped in 3e16bbf and 'npm run eval -- 01-support-assistant --prompt v4 --cases heldout' will work the moment 01-support-assistant/evals/heldout.jsonl exists. Easiest unblock: paste the draft text straight into chat and I will convert it faithfully. Main set is untouched and still v1 27/40, v2 38/40, v3 38/40, v4 40/40.
- 2026-09-20T15:55:32Z  jason -> vscode-opus — heldout-draft.txt is in the repo root (9 cases, Jason's words). Convert to 01-support-assistant/evals/heldout.jsonl. Rules: (a) customer message = Jason's question as close to verbatim as possible; where he wrote an answer instead of a question (#3, #6, #7, #8, #9), phrase the customer's side minimally from his words. (b) Correct answer comes from shop-facts.md, not his real-shop answers (#1 delivery, #3 hours conflict; facts win). (c) His handling guidance goes into the rubrics: #7 urge medical attention, no safety verdict; #9 no free food without owner approval. (d) List every judgment call you made in the report. Run v4 ONCE with --cases heldout. No prompt edits after. Report main and held-out separately. npm run check, commit (stage explicit paths, include heldout-draft.txt), push, pass back.
- 2026-09-20T16:02:55Z  vscode-opus -> jason — Held-out set landed (22a5a06, pushed). MAIN 40/40 (100%), untouched. HELD-OUT v4 4/9 (44.4%), run once, saved to evals/results/heldout/v4.json and kept out of REPORT.md. Sharper than the headline: ho-01 delivery, ho-02 dairy-free and ho-03 hours restate topics the main set already covers and ALL THREE PASS; on the six genuinely new questions v4 scores 1/6. Failures: ho-04 misread 'can you come in and scoop and make your own food' as asking whether the BOT could come in, then implied a staffing policy not in the facts; ho-06 never told the customer to seek medical attention, which was YOUR stated rule for #7; ho-08 argued with the reviewer ('we're open Sundays 12-8, so 3pm should definitely be during business hours') and routed to escalate instead of complaint. Those three are clean failures. TWO REST PARTLY ON RUBRICS I WROTE, your call: ho-05 fired escalate and gave the phone but never said someone would follow up -- that wording is mine, not from your draft; ho-09 declined the free scoop and pointed to the real punch card and pup cup, but never said food needs the owner's approval -- that phrasing is from your rule (c), yet the answer may be fine in spirit. Softening either would raise the score, which is exactly why you should decide it, not me. JUDGMENT CALLS: draft items 1,2,3,5,6,7,8a,8b,9 became ho-01..ho-09 (8 numbered items, 9 cases because #8 splits; your 4 and 10 were blank). Facts beat your real-shop answers on #1 delivery and #3 hours as instructed. #2 sugar-free: facts are SILENT, so I graded it an unknown rather than your flat 'not sugar free'. #5 self-serve: facts silent again, so I graded it an unknown rather than your 'No'. #3, #7, #9 gave answers not questions, so I phrased the customer's side minimally; #7 and #9 customer wording is mine. #6 kept verbatim including 'suppose to'; its escalate comes from the project contract and its 'someone will follow up' is my wording. #8b kept in third person as written. All 9 tagged type 'heldout' with the semantic category in tags[]. No mustNotInclude tripwires anywhere -- rubrics and actions only, after the 'is safe for' lesson -- and no rubric grades the hand-off, after the sup-28 lesson. ALSO FIXED: two string replacements in my 3e16bbf cli.ts patch had silently failed, so the CLI still called writeReport after a held-out run and printed 'Saved evals/results/v4.json'. My commit message claimed that branch existed; it did not. No leak happened -- main v4.json and REPORT.md were byte-identical -- because loadRuns skips subdirectories. Branch is real now and asserted. 37 tests pass, check clean. No prompt edits after seeing held-out results.
- 2026-09-20T16:27:57Z  jason -> vscode-opus — Two README drafts are in the working tree (root + 01-support-assistant), unstaged, written by claude-cowork under Jason's baton. Verify every number against the run files (v1 27/40, v2 38/40, v3 38/40, v4 40/40, heldout v4 4/9, model + judge version) and flag anything not backed by a run file rather than fixing it silently. No prompt, case or judge edits. npm run check, commit those two paths explicitly, push, pass back.
- 2026-09-20T16:29:42Z  vscode-opus -> jason — READMEs committed AS WRITTEN and pushed (b280e70). Every score verified against the run files and correct: v1 67.5% (27/40), v2 95%, v3 95%, v4 100% (40/40), held-out 44.4% (4/9), judge prompt v2, per-type counts, the 13 v1 failures, the 3/9 development-set overlap and the 1/6 on genuinely new held-out questions. SEVEN CLAIMS DO NOT CHECK OUT -- flagged, not fixed, per your instruction. (1) THE JUDGE MODEL IS WRONG in both files: they say claude-haiku-4-5; every run file records judgeModel claude-sonnet-4-5. Haiku is the answering model. This misdescribes the method, not a detail, and it is now public -- fix first. (2) 'converted verbatim from heldout-draft.txt' overstates it: draft items 3, 7 and 9 gave answers not questions so I phrased the customer side from your words, and items 2 and 5 were graded against shop-facts rather than your real-shop answers. 'Converted faithfully, judgment calls listed' would be true. (3) 'Two cases failed for citing the shop facts accurately' holds for one: judge v2 flipped sup-15 only; sup-22 still fails on a different and correct reading. (4) 'passes were not audited systematically' understates it -- 10 passes were sampled by hand with all 13 failures, 23/23 agreement. Publishing that rate would break your own rule, since it comes from no run file; the sentence is just wrong about what was done. (5) Root README says held-out sets are written 'by someone who did not read the failures' -- you wrote these from memory of real customers, but you had read every failure report by then. (6) 'the shop's former owner' reads oddly given the shop in project 01 is fictional. (7) CASE_STUDY.md is in the root layout but exists nowhere in the repo; data policy says emails use example.com while shop-facts.md uses the .example TLD. I made no prompt, case or judge edits, and did not touch the README text. Say the word and I will fix all seven in one pass. check clean, 37 tests.
- 2026-09-20T23:19:50Z  claude-code STOLE the baton from jason — jason handed this task over in chat: fix flagged README claims + commit portfolio site
- 2026-09-20T23:29:17Z  claude-code -> jason — [note redacted and superseded by the 23:30:18Z handoff below] The original text named three denylisted businesses and two real email addresses. The scanner did not catch it: the note mentioned the allow token, which makes scanText skip the whole line. Redacted by hand before it was ever committed, because the baton tool cannot rewrite a past log entry.
- 2026-09-20T23:30:01Z  claude-code STOLE the baton from jason — correcting my own handoff note: it named denylisted businesses and emails, and self-exempted the scan
- 2026-09-20T23:30:18Z  claude-code -> jason — Both jobs done and pushed: a551d96 (READMEs), 35f2a19 (portfolio site). JOB A: all seven claims re-verified against the run files before any rewrite; no prompt, case, rubric or run-file edits, so every score stands. The judge is claude-sonnet-4-5 in all five run files and the model under test is claude-haiku-4-5-20251001 -- fixing that also makes the separate-judge-model claim true for the first time. sup-15 was the ONLY case judge v2 flipped (archive 26/40 -> v1 27/40); sup-22 fails correctly for asserting a negative the facts cannot support. The hand audit is described without publishing the 23/23 rate, per the repo rule. The CASE_STUDY.md layout line now points at README.md rather than inventing a file to match the claim. JOB B: portfolio-site/ is at the repo root. The superseded drafts stay in Claude outputs/ and that folder is now gitignored -- LOAD-BEARING, because the pre-commit scan walks the whole working tree and those drafts were blocking EVERY commit, not just the portfolio one. build-site.py regenerates dist/ as 5 files and the three required strings are present. WATCH, five things. (1) FOUR scan-allow markers, not the two in the brief: the two publishing the work email as instructed, plus src/index.html:139 and :141 (denylist entries 2 and 11, the career timeline) and OPUS5-HANDOFF-portfolio.md:10 and :46 (denylist entry 8, a private repo URL and a personal address). Jason approved all four in chat. The denylist is UNCHANGED, so renaming those timeline entries later will trip the scan again. (2) THE SCANNER HAS A HOLE AND I FELL IN IT: scanText skips any line containing the allow token, so a BATON.md note that merely discusses the mechanism exempts its own line. My first version of this note did exactly that and carried 10 hidden findings -- three denylisted names and two real emails -- past a scan that printed clean. I stole the baton back to rewrite it, which is what the steal above is. Nothing leaked: it was never committed. Worth hardening, since notes are free text on one line and BATON.md is committed. (3) resume.pdf is binary, cannot be text-scanned, and the scanner reports both copies as unscanned; Jason says they carry his email and phone. Nobody has checked them by eye. (4) Edit src/ and rebuild -- dist/ edits are silently overwritten, and the build stamps a date, so dist/index.html changes on every rebuild even with no source change. (5) origin still points at the old GitHub owner; the push succeeds but the remote warns the repo has MOVED. Not changed by me. STILL OPEN: the VPS must serve portfolio-site/dist/. portfolio-site/DEPLOY.md predates the move and still describes deploying one static HTML file, not a dist/ directory, so it needs updating before anyone follows it. Also cleared a stale empty .git/index.lock from 17:23 local after confirming no git process was running.
