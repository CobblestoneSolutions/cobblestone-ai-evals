# Use a DEDICATED key for evals — never the key your production apps use.
# A full run is hundreds of calls; a separate key keeps that load off live systems.
ANTHROPIC_API_KEY=

# Model under test, and the model that grades open-ended answers.
EVAL_MODEL=claude-haiku-4-5-20251001
JUDGE_MODEL=claude-sonnet-4-5
